const excel=require('../../src/reports/generators/excelReportGenerator');const csv=require('../../src/reports/generators/csvReportGenerator');const r={actual_document_name:'a.csv',filed_document_name:'b.csv',status:'MATCHED',match_percentage:100,total_comparisons:1,matched_fields:1,different_fields:0,results:[],exceptions:[]};test('generates excel and csv buffers',()=>{expect(Buffer.isBuffer(excel.buffer(r))).toBe(true);expect(Buffer.isBuffer(csv.buffer(r))).toBe(true)});
